# IDX Stock Analyzer — Python dependencies
# Requires: Python 3.9+  (3.10+ recommended for native X | Y type hints)
# Install:  pip install -r requirements.txt

# Supabase
supabase>=2.3.0

# HTTP / scraping
curl_cffi>=0.6.2          # Browser impersonation for IDX API
requests>=2.31.0

# Market data
yfinance>=0.2.36

# Data processing
pandas>=2.1.0
numpy>=1.26.0

# Config / env
python-dotenv>=1.0.0

# Retry / resilience
tenacity>=8.2.3

# CLI output
rich>=13.7.0              # Pretty tables and progress bars

# Date handling
python-dateutil>=2.8.2

# PDF/Excel parsing (used by shareholders_pdf.py)
openpyxl>=3.1.0           # Required by pandas for .xlsx reading
pdfplumber>=0.10.0        # PDF table extraction (preferred)
# tabula-py>=2.9.0        # Fallback PDF extractor — requires Java; install if pdfplumber fails

# AI (Phase 6)
openai>=1.40.0            # OpenAI API client (default provider)
# anthropic>=0.39.0       # Claude API client (alternative provider)

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
